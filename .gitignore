# credentials — never commit
.env
zk/.env
kraft/.env

# reference files not needed in repo
docker-compose-ref.yml

# saved docker images and offline packages (too large, build locally)
images/
docker-offline/
security-reports/
.DS_Store
